spring:
  cloud:
    gateway:
      routes:
        messaging:
          path: /message/**
          serviceId: sender-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://service_discovery:8761/eureka/
