server:
  port: 8011

spring:
  application:
    name: consumer-service
  cloud:
    config:
      uri: http://config_server:8888
      fail-fast: true
  data:
    mongodb:
      host: mongodb
      database: consumer
      port: 27017
