server:
  port: 8761

spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://config_server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.netflix.eureka: OFF

